# 紋取り（もんどり） Flutter実装 - コードレビュー

**日時**: 2026-09-02  
**対象**: v1.0-alpha 実装完了版  
**ステータス**: ✅ 基本実装完了、📋 最適化・フェーズ1作業待ち

---

## 📊 レビュー結果サマリー

| カテゴリ | 評価 | 詳細 |
|---------|------|------|
| **アーキテクチャ** | ✅ 良好 | 層構造が明確、責務分離OK |
| **ゲームロジック** | ✅ 正確 | v2.0ルール完全実装、動作検証済み |
| **UI/UX** | 📋 改善機会 | 基本機能は動作するが、フィードバック不足 |
| **パフォーマンス** | 📋 最適化機会 | 移動計算に非効率部分あり |
| **テスト** | ❌ なし | ユニット/ウィジェットテスト実装待ち |
| **ドキュメント** | ✅ 充実 | IMPLEMENTATION.md が包括的 |

**全体評価**: 🟡 **プロダクションレディの80%** — Phase 1作業により v1.0達成可能

---

## ✅ 強み

### 1. クリーンなアーキテクチャ
- **責務分離**: Models（ゲームロジック）/ Screens（画面） / Widgets（UI部品）が明確に分離
- **Equatableの適切な使用**: Piece, Position, Board が値比較可能
- **イミュータビリティ**: 全モデルクラスが不変設計（良好な状態管理）

### 2. ゲームルール実装の正確性
- ✅ すべての刻印移動パターン実装済み（進/早/対/王）
- ✅ 奪取・教化ロジック正確
- ✅ ターン管理・勝利条件判定が正確
- ✅ 初期配置（案1）正確に配置

### 3. UI基本機能の完成度
- 6×6盤面の正確な描画
- 駒選択・移動可能位置の視覚化
- 役割別セルカラーコーディング（選択/移動/奪取/教化）

---

## 📋 改善が必要な領域

### 1. **パフォーマンス最適化** (中優先度)

#### Issue: Swift刻印の移動計算が非効率
**File**: `lib/models/piece.dart:118-139`

```dart
// 問題: 全列（a-f）をループして距離チェックしている
for (final col in ['a', 'b', 'c', 'd', 'e', 'f']) {
  if (col != position.column) {
    for (int dist = 1; dist <= 2; dist++) {
      if (col.codeUnitAt(0) == position.column.codeUnitAt(0) + dist ||
          col.codeUnitAt(0) == position.column.codeUnitAt(0) - dist) {
        positions.add(Position(column: col, row: position.row));
      }
    }
  }
}
```

**改善案**:
```dart
// 効率化: 直接左右2マスを計算
for (int dist = 1; dist <= 2; dist++) {
  for (final delta in [-dist, dist]) {
    final newColCode = position.column.codeUnitAt(0) + delta;
    if (newColCode >= 'a'.codeUnitAt(0) && newColCode <= 'f'.codeUnitAt(0)) {
      positions.add(
        Position(column: String.fromCharCode(newColCode), row: position.row)
      );
    }
  }
}
```

**影響**: マイナー（36セル × 30手 / ゲーム ≈ 1000回の計算）

---

### 2. **UI/UXフィードバック不足** (高優先度)

#### Issue A: アニメーション・トランジション欠落
**現状**: 駒の移動が瞬間的
**改善**: 
- [ ] 駒選択時のスケール/色アニメーション
- [ ] 移動時の駒スライドアニメーション
- [ ] 奪取時のフラッシュエフェクト
- [ ] ターン切り替え時のフェードイン/アウト

**参考パッケージ**: `animations ^2.0.0` はすでに依存関係に追加済み

#### Issue B: ゲームの進行状況表示不足
**現状**: ターン数や過去の行動履歴が表示されていない
**改善**:
- [ ] 現在のターン数表示
- [ ] 最後のアクション表示（"プレイヤーAが駒を移動しました"など）
- [ ] ゲーム履歴パネル（タップ展開式）

#### Issue C: ユーザーの誤操作への保護
**現状**: 駒を選択してから無操作時に状態が残る
**改善**:
- [ ] 選択解除ボタン（スペース キーまたはキャンセルボタン）
- [ ] 別の駒をタップ時の自動選択切り替え
- [ ] Undo機能（最後の手を戻す）—— パイルール実装時に必須

---

### 3. **ゲームモード実装欠落** (高優先度)

#### Issue: マルチプレイヤー選択UI なし
**現状**: GameScreen が直接起動され、常にプレイヤーA/B 対戦
**改善**:
- [ ] HomeScreen に「ゲームモード選択」画面追加
  - ホットシートプレイ（1台で交代）
  - リモートプレイ（将来版）
  - AI対戦（将来版）

#### Issue: パイルール UI未実装
**現状**: パイルール実装されていない（IMPLEMENTATION.md Phase 1B で予定）
**改善**:
- [ ] プレイヤーAが初手を実施後、B に確認ダイアログ表示
- [ ] "陣営を交換しますか？" Yes/No 選択
- [ ] 交換時の盤面 90° 回転表示（視覚的インパクト）

---

### 4. **テスト不在** (中優先度)

**現状**: テストコード 0行

**実装予定**:
```
test/
├── models/
│   ├── piece_test.dart         # Position.isValid(), getMovablePositions()
│   └── board_test.dart         # 初期配置, movePiece, capturePiece, convertPiece
├── screens/
│   └── game_screen_test.dart   # ゲームフロー (選択→移動→ターン切り替え)
└── widgets/
    └── board_widget_test.dart  # タップハンドリング
```

**最小テストカバレッジ目標**: 
- ✅ Models 層: 100% (ロジック層はテスト必須)
- ✅ Screens 層: 主要フロー (起動→プレイ→終了)
- 📋 Widgets 層: 主要コンポーネント

---

### 5. **ドキュメント・コメント** (低優先度)

#### Issue: 一部メソッドにコメント不足
**例**: `_getCellColor()` メソッドの戻り値コメント

**改善**: 
- [ ] 各 public メソッドに /// doc コメント追加
- [ ] 複雑な計算（swift 移動など）にインライン説明追加

---

## 🔍 詳細分析

### Models 層の評価

#### ✅ `piece.dart`
- **Piece** クラス: 完璧な実装
- **Position** クラス: 妥当性検証とユーティリティメソッドが良好
- **問題**: swift 計算のみ最適化機会（前述）

#### ✅ `board.dart`
- 不変設計で再レンダリング効率が良い
- 初期配置ファクトリが明確
- `getKingPiece()` で try-catch使用は適切

**提案**: `getKingPiece()` 戻り値が null の場合、ゲーム終了判定につなげる処理が GameScreen にあるため、現状は問題なし

---

### Screens 層の評価

#### ✅ `home_screen.dart`
- 3文ルール説明が L1 法則を完璧に満たす
- 詳細ルールダイアログが充実
- **改善余地**: "ゲームモード選択" 画面への移行（Phase 1B）

#### 📋 `game_screen.dart`
- ゲームロジック実装は正確
- **問題点**:
  1. `selectedPiece` が null でもタップ反応する（無視されるが説明が必要）
  2. ゲーム状態の永続化がない（リセット以外リプレイ不可）
  3. パイルール UI がない

---

### Widgets 層の評価

#### ✅ `board_widget.dart`
- GridView.builder で効率的にレンダリング
- セルカラーコーディングが直感的
- **改善機会**:
  1. 駒の選択時アニメーション（スケール変化）
  2. 移動可能位置の脈動エフェクト（点滅）
  3. 奪取対象の赤ハイライトを border → fill に変更

#### ✅ `_PieceWidget`
- 刻印ラベルが正確
- 影のボックスシャドウで立体感あり
- **改善**: 選択時の blue border を 3px → 5px + glow effect に

---

## 🛠️ Phase 1 実装タスク優先度

### **優先度 1 (v1.0 必須)**
- [ ] **UI改善**: アニメーション・フィードバック
  - 駒選択アニメーション
  - 移動スライド
  - ターン表示更新
  - 所要時間: **3-4時間**

- [ ] **ゲームモード選択**: HomeScreen → GameScreen フロー改善
  - モード選択画面追加
  - リセット後のモード選択へ戻す
  - 所要時間: **2-3時間**

- [ ] **ユニットテスト**: Models 層 100% カバレッジ
  - piece_test.dart (Position, Piece の全メソッド)
  - board_test.dart (初期配置, movePiece, capturePiece, convertPiece)
  - 所要時間: **4-5時間**

### **優先度 2 (v1.1以降推奨)**
- [ ] パイルール UI 実装
- [ ] 初期配置オプション（案2, 案3）
- [ ] ゲーム履歴表示
- [ ] Undo 機能

### **優先度 3 (Phase 2以降)**
- [ ] AI 対戦
- [ ] ネットワーク対戦
- [ ] リプレイ機能

---

## 📝 コード品質メトリクス

```
┌─────────────────────────────────┐
│ Code Quality Assessment (v1.0a) │
├─────────────────────────────────┤
│ Correctness:      ✅ 9/10       │ → ゲームロジック正確、テスト不足で-1
│ Readability:      ✅ 8/10       │ → 一部コメント不足で-2
│ Architecture:     ✅ 9/10       │ → 層構造明確、状態管理良好で-1
│ Performance:      📋 7/10       │ → swift計算最適化で+2できる
│ Maintainability:  ✅ 8/10       │ → テスト不在で-2
│ Documentation:    ✅ 8/10       │ → IMPLEMENTATION.md充実で+1
├─────────────────────────────────┤
│ OVERALL SCORE:    ✅ 8.2/10     │ Phase 1 完成で 9+/10へ
└─────────────────────────────────┘
```

---

## ✅ チェックリスト: Phase 1 実装

### UIアニメーション・フィードバック
- [ ] 駒選択時: ScaleTransition + GestureDetector
- [ ] 移動時: AnimatedPositioned + 200ms アニメーション
- [ ] 奪取フラッシュ: FlashComponent ウィジェット作成
- [ ] ターン表示更新: AnimatedSwitcher で スムーズ切り替え

### ゲームモード
- [ ] GameModeSelectionScreen 作成
- [ ] ローカル 2P 選択UI
- [ ] AI (将来版) プレースホルダー

### テスト
- [ ] test/models/piece_test.dart: 20+ テストケース
- [ ] test/models/board_test.dart: 15+ テストケース
- [ ] CI/CD で `flutter test` 実行

### ドキュメント
- [ ] コード内 doc コメント追加
- [ ] test/ 層の README.md 追加

---

## 🎯 次のステップ

**推奨アプローチ**:

1. **今すぐ** (30分): 
   - swift 移動計算を最適化
   - IMPLEMENTATION.md の Phase 1 チェックリストを確認

2. **本日中** (2-3時間):
   - ユニットテスト実装（models 層）
   - `flutter test` 実行確認

3. **明日以降** (3-4時間):
   - UI アニメーション実装
   - ゲームモード選択画面

4. **週末まで** (1-2時間):
   - 統合テスト・実機確認
   - v1.0-beta リリース準備

---

## 📌 重要な注記

### v2.0ルール検証済みのため
- ゲームロジック変更は不要 ✅
- UI/パフォーマンス改善に集中

### 2026年10月末リリースに向けて
- **優先度 1 タスク完成**: 必須 (v1.0 候補)
- **優先度 2 タスク**: v1.1 で実装可
- **優先度 3 タスク**: Phase 2 (2026年11月以降)

---

**レビュー完了日**: 2026-09-02  
**次回レビュー予定**: v1.0-beta (Phase 1完成時)
